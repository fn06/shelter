echo "Conditions do not get saved!" > out.txt
if (echo "Hmmm" && echo "as you can see" > out.txt) then {
  echo "Then"
  echo "True!" >> out.txt
} else {
  echo "Else"
  echo "False!" >> out.txt
}
echo "out.txt says:" && cat out.txt
