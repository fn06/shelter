; (rule
;   (target opentrace)
;   (action
;     (with-stdout-to opentrace (run echo hello))))

(library
 (name shelter_main)
 (public_name shelter.main)
 (preprocess
  (pps ppx_repr))
 (libraries shelter cid void zfs))

(rule
 (targets raw_flags.ml)
 (action
  (run ./include/discover.exe)))
